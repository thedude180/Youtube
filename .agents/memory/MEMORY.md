- [upload.detected debounce rule](upload-detected-debounce-rule.md) — every upload.detected consumer that schedules AI or YouTube API work MUST have a per-user 30-min debounce map. Missing one → 200-video batch import → N concurrent tasks → quota trip by 6 AM Pacific.
- [Stream-editor stale job backlog](stream-editor-stale-jobs.md) — 4,500+ queued stream_edit_jobs accumulate across container restarts; each runs FFmpeg 90 min → 25-min event-loop stall cycle. Migration 069 cancels queued jobs >24h old on boot. FFMPEG_ENCODE_THREADS capped at 2 (was 4). stdout.resume() drains pipe.
- [Back-catalog quota timing deadlock](back-catalog-quota-deadlock.md) — runner fires into tripped quota window daily, reschedules 22-24h (same window), BF6 catalog never mined; fix: scheduleNextRun() targets getNextResetTime()+5min when breaker active.
- [Migration SQL autopilot_queue no channel_id](migration-autopilot-no-channel-id.md) — autopilot_queue has NO channel_id column; back_catalog_videos uses duration_sec not duration_seconds; any startup migration INSERT into autopilot_queue must omit channel_id; migrations 053+054 had both bugs.
- [Ghost channel FK deletion pattern](ghost-channel-fk-pattern.md) — channel 52 (ET Gaming 247) is referenced by 10 tables; DELETE FROM channels WHERE id=52 always fails with FK constraint; wrap in nested try/catch and mark flag done anyway; same for channel dedup heal DELETE steps in server/index.ts.
- [env-validator AI key names](env-validator-ai-keys.md) — Replit AI integrations inject keys as AI_INTEGRATIONS_OPENAI_API_KEY and AI_INTEGRATIONS_ANTHROPIC_API_KEY, NOT bare OPENAI_API_KEY/ANTHROPIC_API_KEY; env-validator must check the AI_INTEGRATIONS_ prefixed names to avoid false "degraded" warnings on every boot.
- [Admin observability surface](admin-observability.md) — 3 new admin-only routes + 3 SystemHealthPanel sections: boot-registry (T+elapsed per service, convergence detection), vault-health (disk/fail-rate/status-counts), migrations catalog (full 67-entry list with confirmed flags); template-performance-scorer adjusts MKB confidenceScore ±3/cycle via CTR cross-ref.
- [YouTube data cache architecture](youtube-data-cache-arch.md) — single hub service owns all YouTube read API calls; callers use getCachedVideoMetrics/getCachedChannelStats/getCachedChannelCTR; only pipeline-tracer + stream-operator exempt (need live data).
- [Creative Library system](creative-library-system.md) — ever-expanding scored asset library; music tracks auto-seeded from data/music-library/ on every boot; scores via real YouTube Analytics; drives music-scorer track selection; new tables need deleteChannel() entry.
- [kernel-seeds gate disables smart-edit](kernel-seeds-gate.md) — registerSmartEditCommand() was inside isEnabled("kernel-seeds") which is DISABLED; always register smart-edit handler unconditionally outside that block.
- [upload.detected consistency-agent hot-spin](upload-detected-hot-spin.md) — each ingested video fires upload.detected; 2-min delayed consistency check fires per video → N videos ingested N-seconds apart = N checks firing N-seconds apart, burning the background AI slot. Fix: 30-min per-user debounce in _consistencyCheckPendingUntil map.
- [YouTube thumbnail 2MB limit](youtube-thumbnail-2mb.md) — AI-generated 1536×1024 PNG exceeds YouTube's 2MB thumbnails.set limit; always sharp-compress to 1280×720 JPEG @85q before upload; playlist-manager.ts and any future thumbnail upload must check size first.
- [Orchestrator startup delay crash loop](orchestrator-startup-delay-crash.md) — STARTUP_DELAY_MS was 30–40s; orchestrator fired first AI cycle at T+15:30 → 69 OOM crashes/day; fixed to 20–25min after Wave 8 (T+35–40min total)
- [OOM crash loop fixes](oom-crash-fix.md) — 3 root causes of 24-min production crash cycle; all fixed; container memory gate now enforced at ytdlp-gate slot acquisition
- [T+15min convergence crash pattern](t15min-convergence-crash.md) — back-catalog runner + grinder second run + publisher sweep all converge at T+10-20min; fix: memory gates in both + grinder urgent interval raised 10→20 min + removed direct runGrindCycle() from runner
- [ESM-only packages in CJS production bundle](esm-cjs-production-trap.md) — p-limit v5+ and other pure-ESM packages crash the CJS prod build on boot; check `"type": "module"` before importing any package in server code.
- [PDF extraction method](pdf-extraction.md) — attached PDFs must be extracted with `pdftotext <file> -`; direct utf-8 reads fail on binary PDFs; pdftotext is in PATH.
- [Resurrection engine column names](resurrection-engine-targets.md) — content_vault_backups uses `download_error` (not `error_message`) and has no attempts column; pre_encoder_queue / shorts_clip_queue / video_metadata_sync_queue are raw-SQL targets only (no Drizzle table), caught by per-target try/catch. Drizzle wraps PG errors as "Failed query: <SQL>" — catch block must check allText (msg+cause+stack) for "failed query" to silence table-not-found noise.
- [Schema patch 2026-06-03](schema-patch-2026-06-03.md) — ytDlpBackoff + systemSettings tables added; resurrection columns on 5 tables; 6 TS lib files added; live gate cooldown persisted to DB; hardcoded video IDs removed; awaitDbReady() wired; compliance violations integrated.
- [Game focus system](game-focus.md) — getFocusGame/setFocusGame via system_settings key "game_focus:current"; default "Battlefield 6"; MIN_FOCUS_DAYS_AHEAD=30; wired into back-catalog-engine + clip-seo-sync; API at POST /api/youtube/game-focus.
- [Startup migrations pattern](startup-migrations.md) — one-time idempotent boot migrations in server/lib/startup-migrations.ts; wired at T+3s (Wave 0.5) in index.ts; guarded by system_settings boolean flags; production executeSql is READ-ONLY — all production data mutations must use this boot-migration pattern.
- [Channel name fix + game detection](channel-name-game-detection.md) — Channel is "ET Gaming 274" (not 247); Migration 003 in startup-migrations fixes 35+ fake game_names in back_catalog_videos on next prod boot; detectGameFromTitle() expanded for full AC family + more; etgaming247.ts/etgaming247-profile.ts are intentional product-feature names, not channel branding.
- [Self-healing empire services](self-healing-empire.md) — Publishing watchdog uses 3-layer verification (RSS → pipeline_traces → autopilot_queue); channel-intelligence-engine runs every 2h doing zombie detection + queue refill + health score; all wired in index.ts Wave after watchdog.
- [Autonomous OS gating rules](autonomous-os-upgrade.md) — CommandCenter + isProductionAutomationAllowed + AIScheduler must gate every background engine; kill-switch writes require requireAdmin; new tables with channel_id must be added to deleteChannel().
- [Hardened service wiring pattern](hardened-services-wiring.md) — production-guard + token-guardian-hardened + stuck-scheduler-recovery + dead-letter-drain; CommandCenter uses canRun() not requestApproval(); SQL injection middleware added after promptInjectionGuard; new services start at T+90s/T+120s via setTimeout in Wave.
- [Boot-storm crash fix 2026-06-05](boot-storm-crash-fix.md) — 87 crashes/day: T+17min OOM from publisher sweep + back-catalog convergence; fixed by pushing sweep to T+40min/30min; also bumped 4 orchestrator agents + 3 wave5 engines from 20-90s → 5-12min initial delays.
- [AI queue saturation loop fix](ai-queue-saturation.md) — stream-exhaust while loop spun at 4/sec on AI queue full (zero backoff), permanently holding all 4 slots; fixed with re-throw + 30s backoff + boot delays pushed to T+20-27min for stream-exhaust/self-improvement/growth-flywheel.
- [Short schedule saturation cache](schedule-saturation-cache.md) — getNextShortPublishTime does 42 DB queries per call when 14-day queue is full; saturation cache (30 min TTL) + isShortScheduleSaturated guards in all bulk-queue loops prevent event-loop stall + health-check 500s.
- [Long-form schedule saturation cache](longform-saturation-cache.md) — getNextLongFormPublishTime needs mutex+double-checked locking (same as Short); missing mutex → hot-spin → 30 crashes/day. Fix: _longFormMutexTails + withLongFormScheduleMutex + recheck inside mutex.
- [Success DNA compounding system](success-dna-compounding.md) — closed the learning→content feedback loop: refreshSuccessDNA() runs after each daily cycle, extracts winning patterns into channel_success_dna, promotes to masterKnowledgeBank (flows into all AI prompts); Bayesian confidence compounds over time.
- [Multi-signal game detection engine](game-detection-engine.md) — server/services/game-detection.ts; scores title/description/tags via character+weapon+location+keyword dicts; cross-validates BF2042 vs BF6 (Sundance=BF2042); returns null below confidence 38; back-catalog engine uses it first, falls back to legacy extractGameName.
- [Recursive AI loop closure](recursive-ai-loop.md) — promptVersions was write-only (evolved prompts never read back); fixed via prompt-loader.ts + critiqueAndRefine in vod-seo-optimizer + cohort-tracker in learning brain.
- [Publishing stopped root causes](publishing-stopped-root-causes.md) — see topic file; multiple stacked blockers fixed across 2 sessions; most recent: 376 pending items + s0D2BLHmiTU blocker + quota breaker daily trip; all addressed.
- [Quota breaker daily trip mystery](quota-breaker-daily-trip.md) — breaker trips at T+17min after prod boot despite only 857 tracked units (well below 10k limit); callerStack logging added to tripGlobalQuotaBreaker(); publishers DO run at midnight Pacific via quota reset cron regardless.
- [Publisher perpetual-loop hot-spin](publisher-hot-spin.md) — both shorts + long-form perpetual loops treated "all-skipped" batches as "work done" (2s/5s retry); creates infinite tight loop when channel has no OAuth token; fix: add intermediate branch for published==0 && failed==0 → idle backoff (90s/2min).
- [Grinder OOM crashes 2026-06-06](grinder-oom-crashes.md) — 20 crashes/day; 4 root causes fixed: (1) getGrindQueueDepth excluded overdue items via scheduledAt>=now filter → false URGENT mode; (2) 5-min madeProgress follow-up caused T+9min convergence; (3) catalog sync at T+10min too early; (4) VODSEOOptimizer no concurrency limit held 4/4 AI slots.
- [Live stream director architecture](live-stream-director.md) — auto-preps + applies YouTube metadata + pins message + fires afterStreamCopilot; gaps it closed; quota tracking and 45s delay constraints.
- [Job queue thundering herd crash loop](job-queue-thundering-herd.md) — startRecoveryPump() immediate sweep replays stale crash-session jobs, saturating 4 AI background slots → 9 MB/min heap growth → MemoryGuardian restart at T+25min → crash loop.
- [Sequential boot pattern](sequential-boot-pattern.md) — all waves 6–11 now async+sleep; absolute start times T+8/15/20/25/30/35min; publishers listed first in Wave 10; never add AI services to early waves without explicit deferred setTimeout.
- [Saturation cache double-check crash](saturation-cache-double-check.md) — getNextShortPublishTime() fast-path cache check was OUTSIDE the mutex; 18+ Wave 10.5 concurrent callers all queued, each ran the full 42-query scan → hot-spin → health-check 500s → crash loop. Fix: re-check cache inside mutex before advisory lock (double-checked locking).
- [Optimizer hot loop on boot](optimizer-hot-loop.md) — back-catalog meta-update loop iterated ALL catalog videos (~200+) at T+16min without quota check; pushToYouTube() swallowed quota errors without tripping breaker → every iteration burned a full Claude call. Fix: pushToYouTube trips breaker on QUOTA_EXCEEDED; loop capped at 25/cycle + mid-loop breaker check; brand sync same fix.
- [deleteChannel fragile raw-SQL tables](delete-channel-raw-sql.md) — youtube_output_metrics has no channel_id (uses source_video_id); token_vault has no Drizzle schema + doesn't exist in prod. Both were inside the transaction → rollback → 500 on disconnect. Fix pattern: schema tables → loops; non-schema tables → outside tx in try/catch.
- [Reconnect channel cleanup](reconnect-channel-cleanup.md) — handleCallback (youtube.ts) Step 4 deletes all stale youtube/youtubeshorts rows after reconnect; Migration 017 one-time boot cleanup; always use storage.deleteChannel() not raw DELETE.
- [Vault deadlock loop](vault-deadlock-loop.md) — queueVaultDownloadForSource had no "permanent failure" return; both publishers looped forever on undownloadable sources; fix: "download_failed" return + __vault_source_unavailable__ signal + Migration 019 boot cleanup.
- [Daily cap dedup at source](daily-cap-dedup.md) — checkDailyTokenBudget() had zero dedup; hourly cap had 5-min _capWarnedAt guard, daily cap had nothing. Any 2-4s polling loop → [HourlyCap] spam → event loop saturation → crash. Fix: _dailyExhaustedCache Map in token-hourly-cap.ts; self-invalidates at UTC midnight via dateKey mismatch; one-time stack trace on first hit identifies caller. ALL future budget checks fixed automatically.
- [Vault disk-full crash pattern](vault-disk-full-crash.md) — vault fills disk (0.0GB); processVaultDownloads callers re-invoke immediately after break → disk check fires every call with no backoff; sWCir3U6m_U was permanently undownloadable but kept retrying. Fix: _vaultDiskFullUntil 2h backoff cache at < 0.5GB; Migration 021 sets failCount=10 for that video ID.
- [Vault yt-dlp storm crash loop](vault-ytdlp-storm.md) — any vault video that all extractors reject causes a ~40-60 min/round storm; if server crashes before failCount reaches 5, storm never self-limits → 30+ crash cycle. Fix pattern: named migrations (022, 023) set failCount=10 on boot; Migration 034 lowered general sweep to failCount≥3 (server crashes before reaching 5, so threshold of 5 never self-heals).
- [Pre-encoder vault-indexed strict gate](pre-encoder-vault-indexed-gate.md) — vault status=indexed means vault knows the video but hasn't downloaded it; pre-encoder must NEVER attempt section download in this state (stalls gate slot 3+ min each → crash loop). Fix: early return that triggers queueVaultDownloadForSource() then skips. Vault acquireYtdlpSlot(0) = priority 0 (beats section downloads at priority 1).
- [ShortsPublisher no-token hot-loop](shorts-publisher-no-token.md) — when channel has no OAuth token, publisher resets item to scheduled without advancing scheduledAt → picked up again every 90s. Fix: set scheduledAt = NOW() + 4h in the no-token branch so item stays dormant until token returns. Also: ALL services must prefer channels WITH a token (.find(c => c.accessToken)) before falling back — ghost rows (lower DB id) get picked first silently. Fixed in: shorts-publisher, long-form-publisher, stream-operator (2x), stream-editor-auto-publisher, community-auto-manager.
- [yt-dlp event loop stall via SIGTERM](ytdlp-sigterm-stall.md) — execFileAsync timeout:N sends SIGTERM; yt-dlp ignores it when stuck in kernel I/O or when --js-runtimes node child spins → Promise never resolves → Node event loop stalls for hours. Fix: spawnYtDlpWithHardTimeout() uses spawn(detached:true) + process.kill(-pid,'SIGKILL') to kill entire process group; 8-min hard limit.
- [Startup migration updated_at trap](migration-updated-at-trap.md) — content_vault_backups has no updated_at column; any migration SQL using updated_at=NOW() silently fails. Pattern: setFlag must be INSIDE try block or a failed migration marks itself done permanently.
- [permanentFail status leak](permanent-fail-status-leak.md) — migration sets permanentFail:true in metadata but vault entry stays in indexed/downloading; SELECT only checked status, not permanentFail → downloader retries forever. Fix: add permanentFail guard to SELECT + queueVaultDownloadForSource; migration 026 sweeps active-status entries with permanentFail:true.
- [InnerTube all-clients-400 storm](innertube-all-400-storm.md) — when ALL InnerTube clients return HTTP 400, downloadViaInnerTube used to return false → caller fell through to yt-dlp "No video formats found" storm. Fix: count http400FailCount per client; if all clients 400, throw PERM_UNAVAILABLE:HTTP_400_ALL_CLIENTS so caller permanently skips without yt-dlp. Migration 028 sweeps confirmed-inaccessible download_error patterns.
- [InnerTube 401 != video unavailable](innertube-401-vs-400.md) — HTTP 401 (auth expired) must NOT count toward http400FailCount/PERM_UNAVAILABLE; 401 means the Bearer token is bad, not the video is gone. Separate counters: http400FailCount (video unavailable) vs http401FailCount (auth error). 401 → try unauthenticated fallback → if fails, fall through to yt-dlp (NOT PERM_UNAVAILABLE). 283 videos were wrongly perm-skipped; Migration 056 recovers them.
- [Pre-encoder soft-fail Math.max trap](pre-encoder-softfail-loop.md) — `Math.max(prevCount, 1)` for vault-indexed-only format errors kept preEncoderFailCount=1 forever → items never hit the >=3 blacklist gate → infinite retry loop. Fix: always `prevCount + 1`. Migration 055 hard-sets count=3 for already-stuck items.
- [Pre-encoder shelf routing bug](pre-encoder-shelf-routing.md) — auto-clip type was unconditional long-form signal; back-catalog Shorts encoded 16:9 landscape → landed on regular shelf. Fix: isShortContent (contentType) vetoes type-based isLongForm; also dual-field timestamp fallback startSec??segmentStartSec.
- [Full-stream viral moment extraction](viral-moment-extraction.md) — back-catalog engine used evenly-spaced timestamps; 3-tier system: (1) retention curve, (2) transcript AI, (3) even-spacing. Channel is no-commentary so retention curve is primary.
- [Retention curve clip detection](retention-curve-clips.md) — fetchVideoRetentionCurve() in youtube-analytics.ts; extractViralMomentsFromRetentionCurve() in shorts-pipeline-engine.ts; 7-pt smoothing + above-trend score + local maxima; requires video to have analytics data (≥48h + some views).
- [Vision AI clip detector](vision-clip-detector.md) — server/services/vision-clip-detector.ts; GPT-4o watches actual frames (512px JPEG, 60s intervals, 10/batch); acquireAISlotBackground per batch; priority 0 in chain — only fires when vault file exists; returns ViralMoment[] sorted by score.
- [yt-dlp startup-phase stall blind spot](ytdlp-startup-stall.md) — stall watcher reset stalledMs=0 when file not yet created (currentSize===-1); hung yt-dlp in auth/metadata phase ran the full hardTimeoutMs (2h) unchecked. Fix: track startupMs separately; kill after 3 min if file never appears. Also: hardTimeoutMs default reduced 2h→20min for section downloads.
- [Vault 8-min slot starvation crash](vault-slot-starvation.md) — vault entries in indexed/queued state with permanentFail:true in metadata (but status≠failed) hold yt-dlp gate slots for 8min each; stacked video failures converge at T+44min and OOM the container. Fix: migration 031 sweeps ALL permanentFail:true entries to status=failed on boot, making the clip-video-processor preload skip them instantly. Future problematic videos self-heal after MAX_SOFT_RETRIES rounds via clip-video-processor's metadata flag — migration 031's general sweep picks them up on the next restart.
- [PS5 Gameplay fallback title contamination](ps5-game-fallback.md) — 9 services hardcoded "PS5 Gameplay"/"PS5 Gaming" as default gameName when detection fails; produced titles like "Replay: Epic PS5 Gameplay:…" for BF6 streams. Fix: all fallbacks now use getFocusGame(); Migration 032 scrubs existing queue items; perpetual-repair step 5e detects and pushes corrected titles to YouTube for recent VODs.
- [Production analytics timeout pattern](prod-analytics-timeout.md) — YouTube Analytics API takes >15s in prod; AbortSignal.timeout must be ≥30s; analytics-intelligence-engine first scan needs ≥3min warmup after Wave 6 to avoid quota pressure on boot.
- [Wave 11 early-fire convergence crash](wave11-early-fire-convergence.md) — When Wave 10.5 is disabled, Wave 11 fires at T+30.5min not T+35min; combined with grinder/back-catalog/VOD-optimizer causes OOM at T+35min. Fix: Wave 11 sleep 5→15min, VOD optimizer 16→32min, grinder startup 4→10min.
- [Pipeline AI queue cooldown fix](pipeline-ai-queue-cooldown.md) — Pipeline executor must set status='error' (not 'pending') on AI queue full; drip-feed cooldown map resets on crash → pending pipelines flood on every boot.
- [System incident log](system-incident-log.md) — system_incident_log table + server/lib/incident-log.ts; Migration 037 seeds 30 historical incidents; learning brain daily cycle promotes critical/high lessons to masterKnowledgeBank (category="system_lesson"); use logSystemIncident() to add new entries.
- [Human brain learning upgrades](human-brain-learning.md) — 4-part upgrade: harvester now game-aware, brain-association-engine for real-time signal connections, masterKnowledgeBank temporal decay (30d/−3/cycle), weekly synthesis in learning brain.
- [MrBeast acceleration framework](mrbeast-acceleration-framework.md) — 5 services upgraded to learn+act within 2h: hook scorer, acceleration engine, vod-seo brain injection, 24h rapid feedback, back-catalog hook ranking.
- [Pre-encoder section-dl format fallback](pre-encoder-section-dl-format.md) — yt-dlp --download-sections fails "Requested format is not available" for many YouTube videos (no DASH/fragmented formats). Fix: vault-first path — check content_vault_backups for downloaded file; if found, FFmpeg-trim instead. Soft-fail (count=1) when vault=indexed; hard-fail only if vault has no entry. Migration 039 resets previously hard-blacklisted items.
- [Ghost channel query pattern](ghost-channel-query.md) — any query like channels.where(platform='youtube').limit(1) picks ghost row (channel 52, id=lower) before real channel 53. All channel lookups must use .find(c => c.accessToken) or add isNotNull(accessToken) to the DB query. ShortsPrepPipeline had this bug; fixed by ghost channel user_id filter.
- [Null-timestamp auto-clip duplicate trap](null-timestamp-auto-clip-trap.md) — auto-clips without startSec/endSec silently extract 0–60s for all → identical Shorts; vod-long-form without segmentBounds fails "Segment too short (0m)". Pre-encoder now hard-cancels null-timestamp Shorts.
- [Stream edit job multi-platform double-post](stream-edit-multi-platform.md) — stream_edit_jobs with ["youtube","shorts",...] create one studio video per platform; auto-publisher uploads ALL of them as regular YouTube videos → duplicate uploads. Fix: migration strips to ["youtube"] only + auto-publisher dedup via autopilotQueueId.
- [Pre-encoder soft-fail never-increment trap](pre-encoder-softfail-loop.md) — Math.max(prevCount,1) keeps preEncoderFailCount=1 forever for vault-indexed sources; items never excluded (gate is <3); 73+ items for 3 no-DASH-format videos caused 76 OOM crashes/day at T+15min. Fix: Migration 048 sets failCount=3 on all affected items + general vault sweep.
- [Vault fail_count not a column](vault-fail-count-column.md) — content_vault_backups has NO `fail_count` DB column; per-video retry count is in `metadata->>'failCount'` (jsonb). Any migration SQL using `fail_count >= N` will throw "column does not exist" and fail silently. Use `(metadata->>'failCount')::int >= N` instead.
- [Startup migration count-guard trap](startup-migration-count-guard.md) — guarding a seed migration with "skip if cnt>=N existing items" is fragile: the back-catalog engine can regenerate items at 07:05 UTC (quota-reset fallback) immediately after a clear, hitting the guard and preventing seeding. Use a `seededBy` jsonb marker on inserted rows and guard with `WHERE metadata->>'seededBy' = 'migrationXXX'` instead of a raw item count.
- [Studio_auto_publish boot purge NULL trap](studio-boot-purge-null.md) — Boot cleanup used q.source_video_id (always NULL for studio items) in NOT EXISTS → every boot set all studio_auto_publish to permanent_fail → poller re-uploaded endlessly. Fix: COALESCE(metadata->>'studioVideoId'::int, source_video_id). processAutoPublishQueue also excluded 'pending' status — add it to the list.
- [Storm-video preload-race crash loop](storm-video-preload-race.md) — 21 crashes/day (16-min cycle): storm videos bypass permanent-fail because async preload hasn't completed at T+2s when Maximizer fires; 24h expiry re-enables them daily. Three-layer fix: preload gate, neverExpire flag, migration sentinel rows.
- [videos table has no youtube_id column](videos-no-youtube-id.md) — `videos` table holds uploaded/processed files; no `youtube_id` column. YouTube video IDs live in `back_catalog_videos.youtube_video_id`, `content_vault_backups.youtube_id`, and `autopilot_queue.metadata->>'sourceYoutubeId'`.
- [file_size INT4 overflow on large downloads](file-size-int4-overflow.md) — content_vault_backups/studio_videos/asset_library had INT4 file_size (max 2.1 GB); files >2.1 GB download OK but DB update throws overflow → vault record stays un-downloaded. Fixed: bigint in schema + Migration 059 ALTER TABLE.
- [Schedule type filter blind spot](schedule-type-filter.md) — getShortsOnDay/getLastScheduledShortTime/countUploadedShortsForDate only queried ["youtube_short","platform_short"]; "auto-clip" (back-catalog) and "vod-short" were invisible → two back-catalog clips could land in same window. Fixed: add "vod-short","auto-clip" to all three filters.
- [Focus gate bypass mines off-brand games](focus-gate-bypass.md) — when BF6 catalog exhausted, gameFilter was set to null → AC Valhalla/other games queued as Shorts. Fixed: gameFilter=matchesGame always; idle instead of publishing off-brand. Migration 060 purges existing non-BF6 Shorts from queue.
- [One-shot migration race vs per-boot cleanup](one-shot-migration-race.md) — flagged migrations run at T+3s; services regenerate bad items at T+4min; migration flag is set even on rowCount=0 → never re-runs. Content-integrity rules (focus game filter, orphan purge) MUST use non-flagged per-boot cleanups, not one-shot migrations. See cleanupNonBF6QueueItems() as the pattern.
- [Content-maximizer inherits source gameName](content-maximizer-game-inheritance.md) — maximizer reads meta.gameName from source item without validating vs focus game; one mis-labeled catalog video contaminates all generated clips. Fix: regex-test rawGameName against /battlefield|bf6|bf 6/i, fallback to getFocusGame(). Applied in content-maximizer.ts.
- [Catalog rotation all-game check breaks BF6-only](catalog-rotation-bug.md) — allShortsMined/allLfMined used ranked.every() across ALL games; non-BF6 videos never get mined so condition always false → rotation never fires → BF6 mined flags never reset → catalog processing stops after one pass. Fix: filter ranked to bf6Videos before every check; only BF6 IDs in DB reset WHERE clause.
- [Brand-sync and metadata refresh BF6-only gate](brand-sync-game-filter.md) — sweepShortsSEO/sweepReplaySEO/sweepBrandConsistency queried all games; metadata refresh loop used all of ranked. Fixed: gameWhere ILIKE filter added to all 3 sweeps; computeBrandSyncEligibleGame() mirrors queue exhaustion logic (lastOptimizedAt IS NULL check + 2+ streams unlock); metadata loop uses gameFilter.
- [Quota breaker false-trip from internal pre-gate](quota-breaker-false-trip.md) — studio-publisher throws {code:"QUOTA_EXCEEDED"} (string) from canAffordOperation() pre-check; markQuotaErrorFromResponse matched it → breaker killed all publishing at 4,786/10,000 units. Fix: isInternalPreGate guard + UPLOAD_RESERVE 4000→1800.
- [Studio pipeline BF6 gate + double-post fix](studio-bf6-gate.md) — stream-editor-packager creates studio_videos from ANY game the user has streamed; without a BF6 gate it auto-queued AC Valhalla clips → double-posted content already live. 3-layer fix: packager gameName check + scheduleClipsForAutoPublish title check + processAutoPublishQueue last-resort gate (isNonBF6StudioTitle helper). Migration 062 cancels existing non-BF6 studio items on boot.
- [contentType 'long-form' blind spot](contenttype-longform-blindspot.md) — content-maximizer + startup migrations write contentType="long-form" (bare); all publisher/guardian filters only accepted 'long-form-clip'/'vod_long_form'/'long-form-compilation' → long-form publisher always found 0 due items. Fix: add 'long-form' to IN/NOT IN clauses in long-form-clip-publisher, shorts-clip-publisher (cadence gate), perpetual-queue-guardian, server/index.ts Boot+30 redistributor, and startup-migrations migration060 guard.
- [BF6 focus-game filter propagation](bf6-focus-game-filter-propagation.md) — gate semantics: gameName=null → ALLOW (benefit of doubt); matches /battlefield|bf6|bf 6/i → ALLOW; any other gameName → SKIP. Applied to: grinder (grindUserContent+scanForUnderperformers source filter), index.ts maximizer catch-up SQL WHERE, channel-catalog-sync (before maximizer call), upload-watcher (before maximizer call), catalog-content-engine SQL WHERE, trend-wave-interceptor (post-findCatalogMatch check). buildFocusGameRegex from game-focus.ts used for dynamic focus-game changes.
- [Google Cloud Monitoring quota sync](google-quota-sync.md) — google-quota-sync.ts pulls real unit count from Google; activates only when GOOGLE_CLOUD_MONITORING_KEY (service account JSON, Monitoring Viewer role) is set; calibrateQuotaUsage() takes MAX of internal vs Google (never decreases); data is 1–2h delayed.
- [Focus gate slippage patterns](focus-gate-slippage.md) — two bypass paths: (1) queuePastStreamContent had no focus-game gate; (2) gameFilter tests title+gameName but metadata writes only gameName → stale catalog names contaminate queue. Fix: effectiveGameName = detectGameFromTitle ?? gameName; add buildGameFilter check in queuePastStreamContent.
