node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Large media directories (not needed in deployment)
vault/
clips/
reels/
*.mp4
*.mp4.part

# Environment & secrets
.env
.env.local
.env.production

# Replit-specific
.replit
.cache
.config
.upm
.local
replit.md
replit_zip_error_log.txt

# IDE
.idea
*.swp
*.swo
*~

# OS
Thumbs.db
Desktop.ini

# Build artifacts
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

.git.backup.*

# ── Source code exports (full codebase in plain text) ──
CODEBASE_EXPORT.txt
codebase-export.md
youtube-only-codebase.md

# ── AI prompts / internal architecture docs ──
CREATOROS_MASTER_PROMPT_v*.md
CLAUDE (copy).md

# ── Migration snapshots (large JSON, only latest needed) ──
# migrations/meta/*.json
