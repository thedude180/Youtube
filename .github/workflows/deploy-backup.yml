name: Deploy Backup to Render

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Render backup not configured — run manually if needed."
